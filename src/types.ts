export interface Mount {
  id: string;
  name: string;
  description: string;
  image: string;
  price: number;
}

export interface Panel {
  id: string;
  name: string;
  power: number;
  description: string;
  image: string;
  price: number;
}